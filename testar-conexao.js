/*const mysql = require('mysql');


// Configurar as informações de conexão com o banco de dados
const connection = mysql.createConnection({
  host: '127.0.0.1',
  port: '3306',
  user: 'root',
  password: 'elevcom@4815',
  database: 'clienteinformativo',
});

// Conectar ao banco de dados
connection.connect((error) => {
  if (error) {
    console.error('Erro ao conectar ao banco de dados:', error);
  } else {
    console.log('Conexão bem-sucedida com o banco de dados.');
    // Aqui você pode realizar outras operações no banco de dados, se necessário.
    // Por exemplo, executar consultas SQL, inserir registros, etc.
  }
});

// Encerrar a conexão com o banco de dados
connection.end();*/